# Win/Loss Analysis — superseded

This module now defers to the standalone **`win-loss-analyzer`** skill instead of maintaining
its own interview guide, pattern-analysis, and output-format templates here — see the Module
Registry and External Skills section in `SKILL.md`.

That skill handles data intake and classification (CRM exports, call transcripts, interview
notes, survey data), statistical rigor in pattern extraction, and audience-specific reports —
a fuller build-out than what lived in this file.

Keeping one living version there — rather than a duplicate copy in this file — is intentional:
it avoids the two drifting out of sync over time.
